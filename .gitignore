node_modules
# Keep environment variables out of version control
.env

/generated/prisma
# Compiled build output — regenerated by `nest build`, never hand-edited.
/dist

# Real, proprietary lead data — never belongs in version control.
/data/leads/
